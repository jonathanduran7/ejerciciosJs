class Song{
    constructor(title,artist){
        this.title = title;
        this.artist = artist;

        this.audience = []
    }

    howMany(array){
        console.log(array)
    }

}

const song = new Song('dembow', 'Danny Ocean');

const publics = ['joni', 'duran', 'ezequiel']

let letra = 'jonathan'.toUpperCase()

// song.howMany(publics)

console.log(letra)
